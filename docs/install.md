# Install

## Requirements

- PHP 8.3 or newer
- Laravel 13
- Guzzle

## Install the package

```bash
composer require proovit/laravel-proovit
```

## Publish configuration and translations

```bash
php artisan vendor:publish --tag=proovit-config
php artisan vendor:publish --tag=proovit-translations
```
